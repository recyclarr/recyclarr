version: '3'

networks:
  recyclarr:
    name: recyclarr
    external: true

services:
  recyclarr:
    image: ghcr.io/recyclarr/recyclarr
    user: 1000:1000
    build:
      context: .
      args:
        - TARGETPLATFORM=linux/amd64
    init: true
    networks: [recyclarr]
    volumes:
      - ./config:/config
    environment:
      CRON_SCHEDULE: "* * * * *"
      TZ: America/Chicago
